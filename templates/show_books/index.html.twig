{% extends 'base.html.twig' %}

{% block title %}Catalogue de la médiathèque{% endblock %}

{% block body %}

<div class="container">
    <div class="row">
        {% for book in books %}
            {% include "utils/books.html.twig" %} 
        {% endfor %}
    </div>
</div>
{% endblock %}
